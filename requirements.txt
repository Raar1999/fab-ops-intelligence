# Dependencies are declared in pyproject.toml (single source of truth).
# This file is kept as the conventional entry point:
#     pip install -r requirements.txt
# installs the package editable with the dashboard, notebook, and test extras.
-e .[app,notebook,dev]
